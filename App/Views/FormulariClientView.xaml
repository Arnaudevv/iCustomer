<UserControl x:Class="iCustomer.Views.FormulariClientView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:controls="clr-namespace:DLL;assembly=DLL"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">




    <Grid Background="{StaticResource PrimaryDark}">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Form content -->
        <ScrollViewer Grid.Row="0"
                      VerticalScrollBarVisibility="Auto"
                      HorizontalScrollBarVisibility="Disabled">
            <Grid Margin="20">
                <StackPanel Orientation="Vertical" MaxWidth="600">

                    <TextBlock Text="Customer Details"
                               Style="{StaticResource FormTitleStyle}"
                               HorizontalAlignment="Center"/>

                    <Border Height="2" Background="{StaticResource AccentBlue}" Margin="0,0,0,20"/>

                    <!-- ID -->
                    <Grid Margin="0,20" MinHeight="45">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="150"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="ID" Style="{StaticResource FormLabelStyle}"/>
                        <TextBox Grid.Column="1"
                                 Text="{Binding Id}"
                                 IsReadOnly="True"
                                 Margin="20,0,0,0"
                                 Style="{StaticResource FormTextBoxStyle}"/>
                    </Grid>

                    <!-- First Name -->
                    <StackPanel Margin="0,8">
                        <Grid MinHeight="45">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="150"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="Name" VerticalAlignment="Top" Margin="0,10,0,0" Style="{StaticResource FormLabelStyle}"/>
                            <controls:NameControl
                                    CustomerName="{Binding FirstName, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                    IsValid="{Binding FirstNameValid, Mode=TwoWay}"
                                    Grid.Column="1"
                                    Margin="20,0,0,0"
                                    DefaultBorderColor="{StaticResource BorderColor}"
                                    DefaultTextColor="{StaticResource TextPrimary}"
                                    TextBackground="{StaticResource SecondaryDark}"
                                    TextForeground="{StaticResource TextPrimary}"
                                    TextPadding="12,10"
                                    TextFontSize="13"
                                    TextCaretBrush="{StaticResource TextPrimary}"
                                    TextSelectionBrush="{StaticResource AccentBlue}"/>
                        </Grid>
                    </StackPanel>

                    <!-- Last Name -->
                    <StackPanel Margin="0,8">
                        <Grid MinHeight="45">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="150"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="Surname" VerticalAlignment="Top" Margin="0,10,0,0" Style="{StaticResource FormLabelStyle}"/>
                            <controls:NameControl
                                     CustomerName="{Binding LastName, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                     IsValid="{Binding LastNameValid, Mode=TwoWay}"
                                     Grid.Column="1"
                                     Margin="20,0,0,0"
                                     DefaultBorderColor="{StaticResource BorderColor}"
                                     DefaultTextColor="{StaticResource TextPrimary}"
                                     TextBackground="{StaticResource SecondaryDark}"
                                     TextForeground="{StaticResource TextPrimary}"
                                     TextPadding="12,10"
                                     TextFontSize="13"
                                     TextCaretBrush="{StaticResource TextPrimary}"
                                     TextSelectionBrush="{StaticResource AccentBlue}"/>
                        </Grid>
                    </StackPanel>

                    <!-- DNI -->
                    <StackPanel Margin="0,8">
                        <Grid MinHeight="45">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="150"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="DNI" VerticalAlignment="Top" Margin="0,10,0,0" Style="{StaticResource FormLabelStyle}"/>
                            <controls:DniControl
                                     CustomerDni="{Binding Dni, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                     IsValid="{Binding DniValid, Mode=TwoWay}"
                                     Grid.Column="1"
                                     Margin="20,0,0,0"
                                     DefaultBorderColor="{StaticResource BorderColor}"
                                     DefaultTextColor="{StaticResource TextPrimary}"
                                     TextBackground="{StaticResource SecondaryDark}"
                                     TextForeground="{StaticResource TextPrimary}"
                                     TextPadding="12,10"
                                     TextFontSize="13"
                                     TextCaretBrush="{StaticResource TextPrimary}"
                                     TextSelectionBrush="{StaticResource AccentBlue}"/>
                        </Grid>
                        <TextBlock Text="{Binding DniError}" Style="{StaticResource FieldErrorStyle}"/>
                    </StackPanel>

                    <!-- Email -->
                    <StackPanel Margin="0,8">
                        <Grid MinHeight="45">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="150"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="Email" VerticalAlignment="Top" Margin="0,10,0,0" Style="{StaticResource FormLabelStyle}"/>
                            <controls:EmailControl
                                     CustomerEmail="{Binding Email, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                     IsValid="{Binding EmailValid, Mode=TwoWay}"
                                     Grid.Column="1"
                                     Margin="20,0,0,0"
                                     DefaultBorderColor="{StaticResource BorderColor}"
                                     DefaultTextColor="{StaticResource TextPrimary}"
                                     TextBackground="{StaticResource SecondaryDark}"
                                     TextForeground="{StaticResource TextPrimary}"
                                     TextPadding="12,10"
                                     TextFontSize="13"
                                     TextCaretBrush="{StaticResource TextPrimary}"
                                     TextSelectionBrush="{StaticResource AccentBlue}"/>
                        </Grid>
                        <TextBlock Text="{Binding EmailError}" Style="{StaticResource FieldErrorStyle}"/>
                    </StackPanel>

                    <!-- Phone -->
                    <StackPanel Margin="0,8">
                        <Grid MinHeight="45">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="150"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="Phone" VerticalAlignment="Top" Margin="0,10,0,0" Style="{StaticResource FormLabelStyle}"/>
                            <controls:PhoneControl
                                 CustomerPhone="{Binding Phone, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                 IsValid="{Binding PhoneValid, Mode=TwoWay}"
                                 Grid.Column="1"
                                 Margin="20,0,0,0"
                                 DefaultBorderColor="{StaticResource BorderColor}"
                                 DefaultTextColor="{StaticResource TextPrimary}"
                                 TextBackground="{StaticResource SecondaryDark}"
                                 TextForeground="{StaticResource TextPrimary}"
                                 TextPadding="12,10"
                                 TextFontSize="13"
                                 TextCaretBrush="{StaticResource TextPrimary}"
                                 TextSelectionBrush="{StaticResource AccentBlue}"/>
                        </Grid>
                        <TextBlock Text="{Binding PhoneError}" Style="{StaticResource FieldErrorStyle}"/>
                    </StackPanel>

                    <!-- Registration Date -->
                    <Grid Margin="0,8" MinHeight="45">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="150"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="Registration date" Style="{StaticResource FormLabelStyle}"/>
                        <DatePicker Grid.Column="1"
                                    SelectedDate="{Binding RegistrationDate}"
                                    Margin="20,0,0,0"
                                    Style="{StaticResource FormDatePickerStyle}"/>
                    </Grid>

                </StackPanel>
            </Grid>
        </ScrollViewer>

        <!-- Spacer -->
        <Grid Grid.Row="1"/>

        <!-- Action buttons -->
        <StackPanel Grid.Row="2"
                    Orientation="Horizontal"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center"
                    Margin="20,12,20,20">
            <Button Command="{Binding SaveCommand}"
                    IsEnabled="{Binding IsFormValid}"
                    Style="{StaticResource PrimaryActionButtonStyle}"
                    ToolTip="Save changes">
                ✓ Save
            </Button>
            <Button Command="{Binding CancelCommand}"
                    Style="{StaticResource SecondaryActionButtonStyle}"
                    ToolTip="Cancel operation"
                    Margin="12,0,0,0">
                ✕ Cancel
            </Button>
        </StackPanel>
    </Grid>
</UserControl>
